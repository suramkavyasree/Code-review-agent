rules:
  - id: js.xss.innerhtml
    pattern: $OBJ.innerHTML = $VALUE
    message: "Direct DOM assignment to innerHTML — possible XSS. Use textContent or sanitize input."
    severity: ERROR
    languages: [javascript]
